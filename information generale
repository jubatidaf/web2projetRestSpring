-projet realiser par :
		TIDAF Juba
-lien github :
https://github.com/jubatidaf/web2projetRestSpring.git

mode d'utilisation :
-on a deux projets web:
	-sepatrans : c'est le serveur.
	-clientsepatrans : c'est le client.
-pour le fonctionnment:
1-installer la base de données, le script sql se trouve dans le 
repertoire src/main/webapp/resources/ qui se trouve dans le projet sepatrans

NB: login et mot de passe configurer dans se projet pour la base de données est le tuple("root", "").

-2 puis soit lancer les projets sur eclipse en les deployant sur tomacat ou,
on peut copier les fichiers (.war) qui se trouvent dans les rpertoire /traget de chaque projet vers tomcat.

-3 les adresses local :
localhost:8080/sepatrans/
localhost:8080/clientsepatrans/
